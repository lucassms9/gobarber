const { resolve } = require('patch');

modulo.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-patch': resolve(__dirname, 'src', 'app', 'models'),
  'migrations-patch': resolve(__dirname, 'src', 'database', 'migrations'),
  'seeders-patch': resolve(__dirname, 'src', 'database', 'seeds'),
}
